# NoteDraw 配置项文档

## 一、环境变量配置

### 1.1 基础配置

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| NODE_ENV | development | 环境：development/production |
| NEXT_PUBLIC_BASE_URL | http://localhost:3000 | 应用基础URL |
| NEXT_PUBLIC_APP_URL | http://localhost:3001 | 应用URL（备选） |
| NEXT_PUBLIC_DEMO_WEBSITE | false | 是否演示模式 |

### 1.2 数据库配置

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| DATABASE_URL | postgres://keko@127.0.0.1:5432/mksaas | PostgreSQL连接串 |

**连接串格式**：`postgres://[user]:[password]@[host]:[port]/[database]`

### 1.3 认证配置

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| BETTER_AUTH_SECRET | 6dK8yvz4FojGuh/GlX9EcGDucJldqya0HUQ81tAoBeg= | Better Auth 密钥 |
| BETTER_AUTH_URL | http://localhost:3001 | Better Auth URL |

**生成密钥命令**：`openssl rand -base64 32`

### 1.4 OAuth配置

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| GOOGLE_CLIENT_ID | | Google OAuth Client ID |
| GOOGLE_CLIENT_SECRET | | Google OAuth Client Secret |
| GITHUB_CLIENT_ID | | GitHub OAuth Client ID |
| GITHUB_CLIENT_SECRET | | GitHub OAuth Client Secret |

### 1.5 支付配置（Stripe）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| STRIPE_SECRET_KEY | sk_test_xxx | Stripe 密钥 |
| STRIPE_WEBHOOK_SECRET | whsec_xxx | Stripe Webhook 密钥 |
| NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY | pk_test_xxx | Stripe 公钥 |

**价格ID配置**：
| 变量名 | 说明 |
|--------|------|
| NEXT_PUBLIC_STRIPE_PRICE_PRO_MONTHLY | Pro月度订阅价格ID |
| NEXT_PUBLIC_STRIPE_PRICE_PRO_YEARLY | Pro年度订阅价格ID |
| NEXT_PUBLIC_STRIPE_PRICE_LIFETIME | 终身会员价格ID |
| NEXT_PUBLIC_STRIPE_PRICE_CREDITS_BASIC | 基础积分包价格ID |
| NEXT_PUBLIC_STRIPE_PRICE_CREDITS_STANDARD | 标准积分包价格ID |
| NEXT_PUBLIC_STRIPE_PRICE_CREDITS_PREMIUM | 高级积分包价格ID |
| NEXT_PUBLIC_STRIPE_PRICE_CREDITS_ENTERPRISE | 企业积分包价格ID |

### 1.6 支付配置（Creem）

| 变量名 | 说明 |
|--------|------|
| NEXT_PUBLIC_CREEM_PRICE_PRO_MONTHLY | Pro月度订阅价格ID |
| NEXT_PUBLIC_CREEM_PRICE_PRO_QUARTERLY | Pro季度订阅价格ID |
| NEXT_PUBLIC_CREEM_PRICE_PRO_YEARLY | Pro年度订阅价格ID |
| NEXT_PUBLIC_CREEM_PRICE_LIFETIME | 终身会员价格ID |
| NEXT_PUBLIC_CREEM_PRICE_CREDITS_BASIC | 基础积分包价格ID |
| NEXT_PUBLIC_CREEM_PRICE_CREDITS_STANDARD | 标准积分包价格ID |
| NEXT_PUBLIC_CREEM_PRICE_CREDITS_PREMIUM | 高级积分包价格ID |
| NEXT_PUBLIC_CREEM_PRICE_CREDITS_ENTERPRISE | 企业积分包价格ID |

### 1.7 AI服务配置

#### 智谱GLM（文本分析）
| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| GLM_API_KEY | 29108ee8ab0b4e09a4e12a74f0d536b2.c13tNpEYzmdvPZ8U | 智谱API密钥 |
| GLM_BASE_URL | https://open.bigmodel.cn/api/paas/v4 | 智谱API地址 |

#### NanoBanana Gemini（图像生成）
| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| GEMINI_IMAGE_API_KEY | sk-Mqyg5MuJyWBmx1mXGzRLqnux8om4NDpHTOtPhRVAxA8aBYGE | Gemini代理API密钥 |
| GEMINI_IMAGE_BASE_URL | https://api.nanobananai.com/v1beta | Gemini代理地址 |

#### Google AI Studio（备用）
| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| GOOGLE_GENERATIVE_AI_API_KEY | AIzaSyCsqMR98W_lAmDko-urDWJnbrJx29xPWsE | Google AI密钥 |

#### OpenAI/代理服务
| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| OPENAI_API_KEY | sk-Vodwt8kuQwivHjqoMEcQbkc3hdYdGXnzIvkEnB2dUrikizqb | OpenAI/代理API密钥 |
| OPENAI_BASE_URL | https://api.apimart.ai/v1 | API代理地址 |

#### 其他AI服务
| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| REPLICATE_API_TOKEN | | Replicate API Token |
| FAL_KEY | 83463425-7f3c-479e-83cf-57adb00b89d1:5345f98337f4136ab3d5bd90d5ba72de | FAL.ai Key |

### 1.8 代理配置

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| HTTPS_PROXY | http://127.0.0.1:7890 | HTTPS代理（中国用户需要） |

常见代理端口：Clash=7890, V2Ray=10809, Shadowsocks=1080, Surge=6152

### 1.9 邮件配置

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| RESEND_API_KEY | re_JwFNTYt4_Fx5qUeg67ThQBKN5qVSePLpa | Resend邮件API密钥 |

### 1.10 存储配置（S3兼容）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| S3_ACCESS_KEY_ID | | S3访问密钥ID |
| S3_SECRET_ACCESS_KEY | | S3访问密钥 |
| S3_BUCKET_NAME | | S3存储桶名称 |
| S3_ENDPOINT | | S3端点URL |
| S3_PUBLIC_URL | | S3公共访问URL |

### 1.11 分析配置

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| NEXT_PUBLIC_GA_MEASUREMENT_ID | | Google Analytics ID |
| NEXT_PUBLIC_POSTHOG_KEY | | PostHog 公钥 |

### 1.12 NoteDraw业务配置

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| NOTEDRAW_MAX_INPUT_LENGTH | 10000 | 输入文本最大长度 |
| NOTEDRAW_MAX_URL_LENGTH | 10000 | URL抓取最大长度 |
| NOTEDRAW_MIN_INPUT_LENGTH | 10 | 输入文本最小长度 |
| NOTEDRAW_MAX_SECTIONS | 4 | 每张卡片最大Section数 |
| NOTEDRAW_COMPACT_MAX_CARDS | 1 | 精简模式最大卡片数 |
| NOTEDRAW_CREDITS_ANALYSIS | 1 | 内容分析消耗积分 |
| NOTEDRAW_CREDITS_IMAGE | 5 | 图片生成消耗积分 |
| USE_PLACEHOLDER_IMAGE | true | 使用占位图（开发用） |

---

## 二、网站配置（website.tsx）

### 2.1 UI配置

```typescript
ui: {
  mode: {
    defaultMode: 'dark',      // 默认主题模式
    enableSwitch: true,       // 是否允许切换
  },
},
```

### 2.2 元数据配置

```typescript
metadata: {
  images: {
    ogImage: '/og.png',       // Open Graph图片
    logoLight: '/logo.png',   // 浅色Logo
    logoDark: '/logo-dark.png', // 深色Logo
  },
  social: {
    github: 'https://github.com/MkSaaSHQ',
    twitter: 'https://mksaas.link/twitter',
    blueSky: 'https://mksaas.link/bsky',
    discord: 'https://mksaas.link/discord',
    mastodon: 'https://mksaas.link/mastodon',
    linkedin: 'https://mksaas.link/linkedin',
    youtube: 'https://mksaas.link/youtube',
  },
},
```

### 2.3 功能开关

```typescript
features: {
  enableUpgradeCard: true,        // 显示升级卡片
  enableUpdateAvatar: true,       // 允许更新头像
  enableAffonsoAffiliate: false,  // Affonso联盟
  enablePromotekitAffiliate: false, // Promotekit联盟
  enableDatafastRevenueTrack: false, // Datafast收入追踪
  enableCrispChat: process.env.NEXT_PUBLIC_DEMO_WEBSITE === 'true', // Crisp客服
  enableTurnstileCaptcha: process.env.NEXT_PUBLIC_DEMO_WEBSITE === 'true', // Turnstile验证码
},
```

### 2.4 路由配置

```typescript
routes: {
  defaultLoginRedirect: '/admin/dashboard', // 登录后重定向
},
```

### 2.5 分析配置

```typescript
analytics: {
  enableVercelAnalytics: false,   // Vercel Analytics
  enableSpeedInsights: false,     // Vercel Speed Insights
},
```

### 2.6 认证配置

```typescript
auth: {
  enableGoogleLogin: false,       // Google登录
  enableGithubLogin: false,       // GitHub登录
  enableCredentialLogin: true,    // 账号密码登录
},
```

### 2.7 国际化配置

```typescript
i18n: {
  defaultLocale: 'en',            // 默认语言
  locales: {
    en: {
      flag: '🇺🇸',
      name: 'English',
      hreflang: 'en',
    },
    zh: {
      flag: '🇨🇳',
      name: '中文',
      hreflang: 'zh-CN',
    },
  },
},
```

### 2.8 博客配置

```typescript
blog: {
  enable: true,                   // 启用博客
  paginationSize: 6,              // 每页文章数
  relatedPostsSize: 3,            // 相关文章数
},
```

### 2.9 文档配置

```typescript
docs: {
  enable: false,                  // 启用文档
},
```

### 2.10 邮件配置

```typescript
mail: {
  provider: 'resend',             // 邮件服务商
  fromEmail: 'NoteDraw <onboarding@resend.dev>',
  supportEmail: 'NoteDraw <onboarding@resend.dev>',
},
```

### 2.11 Newsletter配置

```typescript
newsletter: {
  enable: false,                  // 启用订阅
  provider: 'resend',             // 服务商
  autoSubscribeAfterSignUp: false, // 注册后自动订阅
},
```

### 2.12 存储配置

```typescript
storage: {
  enable: true,                   // 启用存储
  provider: 's3',                 // 存储服务商
},
```

### 2.13 支付配置

```typescript
payment: {
  provider: 'creem',              // 支付服务商：stripe/creem
},
```

---

## 三、价格计划配置

### 3.1 免费计划

```typescript
free: {
  id: 'free',
  prices: [],
  isFree: true,
  isLifetime: false,
  credits: {
    enable: true,
    amount: 50,          // 每月50积分
    expireDays: 30,      // 30天过期
  },
},
```

### 3.2 Pro计划

```typescript
pro: {
  id: 'pro',
  prices: [
    {
      type: PaymentTypes.SUBSCRIPTION,
      priceId: 'STRIPE_PRICE_ID',
      creemPriceId: 'CREEM_PRICE_ID',
      amount: 4900,       // $49/月
      currency: 'USD',
      interval: PlanIntervals.MONTH,
    },
    {
      type: PaymentTypes.SUBSCRIPTION,
      priceId: 'STRIPE_PRICE_ID',
      creemPriceId: 'CREEM_PRICE_ID',
      amount: 8700,       // $87/季
      currency: 'USD',
      interval: PlanIntervals.QUARTER,
    },
    {
      type: PaymentTypes.SUBSCRIPTION,
      priceId: 'STRIPE_PRICE_ID',
      creemPriceId: 'CREEM_PRICE_ID',
      amount: 22800,      // $228/年
      currency: 'USD',
      interval: PlanIntervals.YEAR,
    },
  ],
  isFree: false,
  isLifetime: false,
  popular: true,          // 推荐标签
  credits: {
    enable: true,
    amount: 1000,         // 每月1000积分
    expireDays: 30,
  },
},
```

### 3.3 终身计划

```typescript
lifetime: {
  id: 'lifetime',
  prices: [
    {
      type: PaymentTypes.ONE_TIME,
      priceId: 'STRIPE_PRICE_ID',
      creemPriceId: 'CREEM_PRICE_ID',
      amount: 19900,      // $199 一次性
      currency: 'USD',
      allowPromotionCode: true,
    },
  ],
  isFree: false,
  isLifetime: true,
  disabled: true,         // 当前禁用
  credits: {
    enable: true,
    amount: 1000,         // 每月1000积分
    expireDays: 30,
  },
},
```

---

## 四、积分包配置

### 4.1 基础包（Basic）

```typescript
basic: {
  id: 'basic',
  popular: false,
  amount: 100,            // 100积分
  expireDays: 30,
  price: {
    priceId: 'STRIPE_PRICE_ID',
    creemPriceId: 'CREEM_PRICE_ID',
    amount: 990,          // $9.9
    currency: 'USD',
    allowPromotionCode: true,
  },
},
```

### 4.2 标准包（Standard）

```typescript
standard: {
  id: 'standard',
  popular: true,          // 推荐
  amount: 200,            // 200积分
  expireDays: 30,
  price: {
    priceId: 'STRIPE_PRICE_ID',
    creemPriceId: 'CREEM_PRICE_ID',
    amount: 1490,         // $14.9
    currency: 'USD',
    allowPromotionCode: true,
  },
},
```

### 4.3 高级包（Premium）

```typescript
premium: {
  id: 'premium',
  popular: false,
  amount: 500,            // 500积分
  expireDays: 30,
  price: {
    priceId: 'STRIPE_PRICE_ID',
    creemPriceId: 'CREEM_PRICE_ID',
    amount: 3990,         // $39.9
    currency: 'USD',
    allowPromotionCode: true,
  },
},
```

### 4.4 企业包（Enterprise）

```typescript
enterprise: {
  id: 'enterprise',
  popular: false,
  amount: 1000,           // 1000积分
  expireDays: 30,
  price: {
    priceId: 'STRIPE_PRICE_ID',
    creemPriceId: 'CREEM_PRICE_ID',
    amount: 6990,         // $69.9
    currency: 'USD',
    allowPromotionCode: true,
  },
},
```

---

## 五、积分配置

```typescript
credits: {
  enableCredits: true,                // 启用积分系统
  enablePackagesForFreePlan: false,   // 免费用户可购买积分包
  registerGiftCredits: {
    enable: true,
    amount: 50,                       // 注册赠送50积分
    expireDays: 30,                   // 30天过期
  },
  packages: { ... },                  // 积分包配置（见上文）
},
```

---

## 六、NoteDraw业务配置

### 6.1 文本限制（src/config/notedraw.ts）

```typescript
export const TEXT_LIMITS = {
  MAX_INPUT_LENGTH: parseInt(process.env.NOTEDRAW_MAX_INPUT_LENGTH || '10000', 10),
  MAX_URL_CONTENT_LENGTH: parseInt(process.env.NOTEDRAW_MAX_URL_LENGTH || '10000', 10),
  MIN_INPUT_LENGTH: parseInt(process.env.NOTEDRAW_MIN_INPUT_LENGTH || '10', 10),
};
```

### 6.2 卡片限制

```typescript
export const CARD_LIMITS = {
  MAX_SECTIONS_PER_CARD: parseInt(process.env.NOTEDRAW_MAX_SECTIONS || '4', 10),
  COMPACT_MODE_MAX_CARDS: parseInt(process.env.NOTEDRAW_COMPACT_MAX_CARDS || '1', 10),
};
```

### 6.3 积分消耗

```typescript
export const CREDIT_COSTS = {
  ANALYSIS: parseInt(process.env.NOTEDRAW_CREDITS_ANALYSIS || '1', 10),
  IMAGE_GENERATION: parseInt(process.env.NOTEDRAW_CREDITS_IMAGE || '5', 10),
};
```

---

## 七、AI模型配置

### 7.1 支持的API提供商

```typescript
type ApiProvider = 'gemini' | 'apimart' | 'fal' | 'replicate' | 'openai' | 'custom';
```

### 7.2 支持的图像模型

```typescript
type ImageModel =
  | 'gemini-2.0-flash-preview-image-generation'
  | 'gpt-4o-image'
  | 'gemini-3-pro-image-preview'
  | 'flux-pro'
  | 'dall-e-3';
```

### 7.3 支持的文本模型

```typescript
type TextModel = 'gpt-4o-mini' | 'gpt-4o' | 'deepseek-chat' | 'glm-4-flash';
```

### 7.4 默认AI配置

```typescript
const DEFAULT_AI_CONFIG = {
  apiProvider: 'gemini',
  imageModel: 'gemini-2.0-flash-preview-image-generation',
  textModel: 'glm-4-flash',
  usePlaceholder: false,
  customProvider: undefined,
};
```

---

## 八、轮询配置

```typescript
const INITIAL_POLL_INTERVAL = 1000;  // 初始轮询间隔：1秒
const MAX_POLL_INTERVAL = 5000;      // 最大轮询间隔：5秒
const MAX_POLL_ATTEMPTS = 30;        // 最大轮询次数：30次
```

---

## 九、Session配置

```typescript
session: {
  cookieCache: {
    enabled: true,
    maxAge: 60 * 60,              // 缓存1小时
  },
  expiresIn: 60 * 60 * 24 * 7,    // 会话7天过期
  updateAge: 60 * 60 * 24,        // 每天更新一次
  freshAge: 0,                     // 禁用新鲜度检查
},
```

---

## 十、环境变量模板（.env.local.bak）

```bash
# ============================================================
# NoteDraw V2 - 环境变量配置
# ============================================================

# ============ 基础配置 ============
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3001

# ============ 数据库 ============
DATABASE_URL=postgresql://postgres:password@host:6543/postgres

# ============ 认证 ============
BETTER_AUTH_SECRET=YOUR_SECRET_HERE
BETTER_AUTH_URL=http://localhost:3001

# ============ OAuth [可选] ============
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ============ 支付 Stripe [可选] ============
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# ============ AI 服务 ============
# 智谱 GLM 文本模型
GLM_API_KEY=YOUR_GLM_API_KEY
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# NanoBanana 图像生成 API
GEMINI_IMAGE_API_KEY=YOUR_GEMINI_API_KEY
GEMINI_IMAGE_BASE_URL=https://api.nanobananai.com/v1beta

# OpenAI 代理服务（备用）
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
OPENAI_BASE_URL=https://api.apimart.ai/v1

# ============ 邮件 ============
RESEND_API_KEY=YOUR_RESEND_API_KEY

# ============ 存储 [可选] ============
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
S3_ENDPOINT=
S3_PUBLIC_URL=

# ============ 分析 [可选] ============
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_POSTHOG_KEY=

# ============ 其他 ============
NEXT_PUBLIC_DEMO_WEBSITE=false

# ============ 开发模式 ============
USE_PLACEHOLDER_IMAGE=true
```
